GOOGLE_BOOKS_API_KEY=

# Supabase (Project Settings -> API for URL/anon/service_role; Project -> Connect for DATABASE_URL)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# If your DB password has special characters ($, @, :, /, etc.), percent-encode them
# (e.g. $ -> %24) or the connection will fail or silently use the wrong password.
DATABASE_URL=
